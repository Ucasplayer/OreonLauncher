appId: 'SSMP Launcher'
productName: 'SSMP Launcher'
artifactName: '${productName}-setup-${version}.${ext}'

copyright: 'Copyright © 2025-2026 60zz'

asar: true
compression: 'maximum'

files:
  - '!{dist,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,.eslintrc.json}'

extraResources:
  - 'libraries'
  - 'lang'

# Windows Configuration
win: 
  target: 
    - target: 'nsis'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: '60zz'
  vendor: '60zz'
  synopsis: 'SSMP Launcher'
  description: 'Oficial Launcher SSMP minecraft server'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'